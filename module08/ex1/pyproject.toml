[tool.poetry]
name = "check-dependencies"
version = "0.1.0"
description = "Loading programs: dependency check and Matrix data analysis"
authors = ["mbuchet"]

[tool.poetry.dependencies]
python = "^3.10"
numpy = "^2.2.6"
matplotlib = "^3.10.9"
pandas = "^2.3.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
